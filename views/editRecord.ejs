<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
  <title>Edit Record</title>
</head>
<body>
  <div class="container mt-5">
    <h1 class="text-center mb-4">Edit Record</h1>
    <div class="card">
      <div class="card-header bg-primary text-white">Edit Record Details</div>
      <div class="card-body">
        <form action="/updateRecord/<%= record._id %>" method="post" enctype="multipart/form-data">
          <div class="mb-3">
            <label for="name" class="form-label">Name</label>
            <input type="text" class="form-control" name="name" value="<%= record.name %>" required>
          </div>
          <div class="mb-3">
            <label for="email" class="form-label">Email</label>
            <input type="email" class="form-control" name="email" value="<%= record.email %>" required>
          </div>
          <div class="mb-3">
            <label for="date" class="form-label">Update Date</label>
            <input type="date" class="form-control" name="createDate" value="<%= record.createDate %>" required>
          </div>
          <div class="mb-3">
            <label for="phone" class="form-label">Phone</label>
            <input type="text" class="form-control" name="phone" value="<%= record.phone %>" required>
          </div>
          <div class="mb-3">
            <label for="image" class="form-label">Update Image</label>
            <input type="file" class="form-control" name="image">
          </div>
          <div class="mb-3">
            <label for="status" class="form-label">Status</label>
            <select class="form-select" name="status">
              <option value="true" <%= record.status ? 'selected' : '' %>>Active</option>
              <option value="false" <%= !record.status ? 'selected' : '' %>>Inactive</option>
            </select>
          </div>
          <button type="submit" class="btn btn-success">Update</button>
          <a href="/" class="btn btn-secondary">Cancel</a>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
